plugins {
    // Auto-provisions a compatible JDK 17 toolchain when the host JDK differs.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "thalovant-sdk"
